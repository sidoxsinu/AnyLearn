diff --git a/next.config.ts b/next.config.ts
index 5018f92..62a6047 100644
--- a/next.config.ts
+++ b/next.config.ts
@@ -2,9 +2,8 @@ import type { NextConfig } from "next";
 
 const nextConfig: NextConfig = {
   reactStrictMode: true,
-  typescript: {
-    ignoreBuildErrors: true,
-  },
+  // Required so that Zustand doesn't SSR-error on localStorage access
+  // (all store usage is behind 'use client')
 };
 
 export default nextConfig;
diff --git a/src/app/quiz/[lessonId]/page.tsx b/src/app/quiz/[lessonId]/page.tsx
index 3574768..5d5e5b3 100644
--- a/src/app/quiz/[lessonId]/page.tsx
+++ b/src/app/quiz/[lessonId]/page.tsx
@@ -9,7 +9,7 @@ import { generate, ApiKeyStore } from '@/lib/llmClient';
 import { Prompts } from '@/lib/prompts';
 import type { Question, QuestionOption, Course, Lesson, RoadmapPatch } from '@/lib/models';
 
-export function createRemedialPatch(
+function createRemedialPatch(
   course: Course,
   lesson: Lesson,
   weakConcepts: string[]
diff --git a/src/app/report/[lessonId]/page.tsx b/src/app/report/[lessonId]/page.tsx
index 45543c8..637a857 100644
--- a/src/app/report/[lessonId]/page.tsx
+++ b/src/app/report/[lessonId]/page.tsx
@@ -7,7 +7,7 @@ import { generate, ApiKeyStore } from '@/lib/llmClient';
 import { Prompts } from '@/lib/prompts';
 import type { FixPlan, VerifyResult, Lesson, PatchOp } from '@/lib/models';
 
-export function constructAfterLesson(
+function constructAfterLesson(
   lesson: Lesson,
   patchOps: PatchOp[],
   lessonID: string
diff --git a/src/components/AppShell/AvatarDropdown.tsx b/src/components/AppShell/AvatarDropdown.tsx
index 7aebfa8..b2bda85 100644
--- a/src/components/AppShell/AvatarDropdown.tsx
+++ b/src/components/AppShell/AvatarDropdown.tsx
@@ -64,7 +64,7 @@ export function AvatarDropdown() {
           <div className="px-3 py-2 border-b border-white/10">
             <p className="text-xs font-medium text-white/60">ACTIVE LEARNER</p>
             <p className="text-sm font-bold truncate text-white">
-              {course ? course.title : 'Ready to Learn'}
+              {course ? (course.profile?.topic || course.goal) : 'Ready to Learn'}
             </p>
           </div>
 
diff --git a/src/components/AppShell/FloatingToolbar.tsx b/src/components/AppShell/FloatingToolbar.tsx
index d7a57a1..878d6b1 100644
--- a/src/components/AppShell/FloatingToolbar.tsx
+++ b/src/components/AppShell/FloatingToolbar.tsx
@@ -6,14 +6,14 @@ import { FluentEmoji } from '@/components/FluentEmoji';
 
 export function FloatingToolbar() {
-  const { course, currentLessonID, completedLessons } = useStore();
+  const { course, learner } = useStore();
+  const completedLessonIDs = learner?.completedLessonIDs ?? [];
 
   // Determine active lesson and quiz targets
-  const allLessons = course?.modules.flatMap((m) => m.lessons) || [];
+  const allLessonIDs = course?.modules.flatMap((m) => m.lessonIDs) ?? (course ? Object.keys(course.lessons) : []);
   const targetLessonID =
-    currentLessonID ||
-    allLessons.find((l) => !completedLessons?.includes(l.id))?.id ||
-    allLessons[0]?.id;
+    allLessonIDs.find((id) => !completedLessonIDs.includes(id)) ||
+    allLessonIDs[0];
 
   const lessonHref = targetLessonID ? `/lesson/${targetLessonID}` : '/roadmap';
   const quizHref = targetLessonID ? `/quiz/${targetLessonID}` : '/roadmap';
diff --git a/src/components/AppShell/SecondaryPanel.tsx b/src/components/AppShell/SecondaryPanel.tsx
index e46db41..3a9d949 100644
--- a/src/components/AppShell/SecondaryPanel.tsx
+++ b/src/components/AppShell/SecondaryPanel.tsx
@@ -6,16 +6,19 @@ import { FluentEmoji } from '@/components/FluentEmoji';
 
 export function SecondaryPanel() {
-  const { course, completedLessons } = useStore();
+  const { course, learner } = useStore();
+  const completedLessonIDs = learner?.completedLessonIDs ?? [];
 
-  const totalLessons = course ? course.modules.reduce((acc, m) => acc + m.lessons.length, 0) : 0;
-  const completedCount = completedLessons ? completedLessons.length : 0;
+  const totalLessons = course ? course.modules.reduce((acc, m) => acc + (m.lessonIDs?.length || 0), 0) : 0;
+  const completedCount = completedLessonIDs.length;
   const upcomingCount = Math.max(0, totalLessons - completedCount);
 
-  // Find next uncompleted lesson
-  const nextLesson = course?.modules
-    .flatMap((m) => m.lessons)
-    .find((l) => !completedLessons?.includes(l.id));
+  // Find next uncompleted lesson
+  const nextLesson = course
+    ? course.modules
+        .flatMap((m) => m.lessonIDs)
+        .map((id) => course.lessons[id])
+        .find((l) => l && !completedLessonIDs.includes(l.id))
+    : undefined;
 
   return (
     <aside
@@ -39,5 +42,5 @@ export function SecondaryPanel() {
           <div className="bg-white rounded-2xl p-4 shadow-dei-card border border-black/5">
             <span className="text-xs font-semibold text-black/60 uppercase tracking-wider">Current Goal</span>
-            <h3 className="text-base font-extrabold mt-1 text-black leading-snug">{course.title}</h3>
+            <h3 className="text-base font-extrabold mt-1 text-black leading-snug">{course.profile?.topic || course.goal}</h3>
             <p className="text-xs text-black/70 mt-1 line-clamp-2">{course.goal}</p>
           </div>
